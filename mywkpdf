# find HTML/*.html | sed -e 's/HTML\/\(.*\)\.html/\1/g' | xargs -I {} scripts/mywkpdf --source /Users/xeno_by/Documents/HTML/{}.html --stylesheet-media screen --print-background yes --output {}.pdf

#!/usr/bin/env bash
if [[ -f $8 ]]; then
  echo rm $2
  rm $2
else
  echo wkpdf "$*"
  wkpdf $*
fi